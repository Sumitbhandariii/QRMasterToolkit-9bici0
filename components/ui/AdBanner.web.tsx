// Web stub — AdMob banners are native-only
import React from 'react';
import { View } from 'react-native';

interface Props {
  bottomSafe?: boolean;
}

export function AdBanner(_props: Props) {
  return <View />;
}
